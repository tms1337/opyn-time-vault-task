{
  "name": "opyn-time-vault",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "contracts": "cd packages/contracts && yarn hardhat compile",
    "frontend": "cd packages/frontend && yarn start",
    "shared": "cd packages/shared"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.1.3"
  }
}
